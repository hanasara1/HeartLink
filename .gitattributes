# 기본: 텍스트 파일은 Git이 자동 판단, 커밋 시 LF로 정규화
* text=auto

# 셸 스크립트는 항상 LF (Linux 실행 호환)
*.sh text eol=lf

# Windows 배치/PowerShell은 CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

# 바이너리는 변환 금지 (모델 가중치, 데이터 등)
*.pt binary
*.joblib binary
*.index binary
*.npy binary
*.pdf binary
*.dat binary
